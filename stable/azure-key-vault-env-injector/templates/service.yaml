apiVersion: v1
kind: Service
metadata:
  name: {{ template "azure-key-vault-to-kubernetes.fullname" . }}
  labels:
    app: {{ template "azure-key-vault-to-kubernetes.fullname" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    component: mutating
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: {{ .Values.service.name }}
      port: {{ .Values.service.externalPort }}
      targetPort: {{ .Values.service.internalPort }}
      protocol: TCP
    - name: {{ .Values.authService.name }}
      port: {{ .Values.authService.externalPort }}
      targetPort: {{ .Values.authService.internalPort }}
      protocol: TCP
  selector:
    app: {{ template "azure-key-vault-to-kubernetes.name" . }}
    release: {{ .Release.Name }}
