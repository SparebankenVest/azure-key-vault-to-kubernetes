{{- $dot := . }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "azure-key-vault-to-kubernetes.fullname" . }}
  labels:
    app: {{ template "azure-key-vault-to-kubernetes.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    component: webhook
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ template "azure-key-vault-to-kubernetes.name" . }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ template "azure-key-vault-to-kubernetes.name" . }}
        release: {{ .Release.Name }}
        {{- if .Values.keyVault.customAuth.enabled }}
          {{- if and .Values.keyVault.customAuth.autoInject.enabled .Values.keyVault.customAuth.autoInject.podIdentitySelector }}
        aadpodidbinding: {{ .Values.keyVault.customAuth.autoInject.podIdentitySelector }}
          {{- end}}
        {{- end}}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/apiservice.yaml") . | sha256sum }}
    spec:
      serviceAccountName: {{ template "azure-key-vault-to-kubernetes.fullname" . }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          env:
          - name: TLS_CERT_FILE
            value: /var/serving-cert/servingCert
          - name: TLS_PRIVATE_KEY_FILE
            value: /var/serving-cert/servingKey
          - name: TLS_CA_FILE
            value: /var/serving-cert/caCert
          - name: TLS_CLIENT_FILE
            value: /var/client-cert/clientCert
          - name: TLS_CLIENT_KEY_FILE
            value: /var/client-cert/clientKey
          - name: AZUREKEYVAULT_ENV_IMAGE
            value: {{ .Values.envImage.repository }}:{{ .Values.envImage.tag }}
          - name: WEBHOOK_AUTH_SERVICE
            value: {{ template "azure-key-vault-to-kubernetes.fullname" . }}
          - name: WEBHOOK_AUTH_SERVICE_PORT
            value: {{ .Values.authService.externalPort | quote }}
          - name: AAD_POD_BINDING_LABEL
            valueFrom:
              fieldRef:
                fieldPath:  metadata.labels['aadpodidbinding']
          {{- if .Values.keyVault.customAuth.enabled }}
          - name: CUSTOM_AUTH
            value: {{ .Values.keyVault.customAuth.enabled | quote }}
            {{- if .Values.keyVault.customAuth.autoInject.enabled }}
          - name: CUSTOM_AUTH_INJECT
            value: {{ .Values.keyVault.customAuth.autoInject.enabled | quote }}
            {{- end}}
            {{- if .Values.keyVault.customAuth.autoInject.secretName }}
          - name: CUSTOM_AUTH_INJECT_SECRET_NAME
            value: {{ .Values.keyVault.customAuth.autoInject.secretName | quote }}
            {{- end}}
          {{- end}}
          - name: LOG_LEVEL
            value: {{ .Values.log_level | quote }}
          - name: METRICS_ENABLED
            value: {{ .Values.metrics.enabled | quote }}
          - name: METRICS_ADDR
            value: {{ .Values.metrics.address | quote }}
          {{- range $key, $value := .Values.env }}
          - name: {{ $key }}
            valueFrom:
              secretKeyRef:
                name: {{ template "azure-key-vault-to-kubernetes.fullname" $dot }}-env
                key: {{ $key }}
          {{- end }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
          - name: webhook
            containerPort: {{ .Values.service.internalPort }}
          volumeMounts:
          - mountPath: /var/serving-cert
            name: serving-cert
          - mountPath: /var/client-cert
            name: client-cert
          - mountPath: /var/run/docker.sock
            name: dockersock
            readOnly: true
          {{- if not .Values.keyVault.customAuth.enabled }}
          - mountPath: /etc/kubernetes/azure.json
            name: azureconf
            readOnly: true
          {{- end }}
          resources:
{{ toYaml .Values.resources | indent 12 }}
        - name: {{ .Values.authService.containerName }}
          image: "{{ .Values.authService.image.repository }}:{{ .Values.authService.image.tag }}"
          env:
          - name: TLS_CERT_FILE
            value: /var/serving-cert/servingCert
          - name: TLS_PRIVATE_KEY_FILE
            value: /var/serving-cert/servingKey
          - name: TLS_CA_FILE
            value: /var/serving-cert/caCert
          - name: WEBHOOK_AUTH_SERVICE
            value: {{ template "azure-key-vault-to-kubernetes.fullname" . }}
          - name: WEBHOOK_AUTH_SERVICE_PORT
            value: {{ .Values.authService.port.externalPort | quote }}
          {{- if .Values.keyVault.customAuth.enabled }}
          - name: CUSTOM_AUTH
            value: {{ .Values.keyVault.customAuth.enabled | quote }}
          {{- end}}
          - name: LOG_LEVEL
            value: {{ .Values.log_level | quote }}
          ports:
          - name: auth
            containerPort: {{ .Values.authService.port.internalPort }}
          volumeMounts:
          - mountPath: /var/serving-cert
            name: serving-cert
          {{- if not .Values.keyVault.customAuth.enabled }}
          - mountPath: /etc/kubernetes/azure.json
            name: azureconf
            readOnly: true
          {{- end }}
      volumes:
      - name: serving-cert
        secret:
          defaultMode: 420
          secretName: {{ template "azure-key-vault-to-kubernetes.fullname" . }}
      - name: client-cert
        secret:
          defaultMode: 420
          secretName: {{ template "azure-key-vault-to-kubernetes.fullname" . }}-client
      - name: dockersock
        hostPath:
          path: /var/run/docker.sock
          type: File
      {{- if not .Values.keyVault.customAuth.enabled }}
      - name: azureconf
        hostPath:
          path: /etc/kubernetes/azure.json
          type: File
      {{- end }}
    {{- if .Values.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.nodeSelector | indent 8 }}
    {{- end }}
  {{- if .Values.tolerations }}
      tolerations:
{{ toYaml .Values.tolerations | indent 8 }}
  {{- end }}
  {{- if .Values.affinity }}
      affinity:
{{ toYaml .Values.affinity | indent 8 }}
  {{- end }}
