apiVersion: v1
kind: Service
metadata:
  name: {{ template "azure-key-vault-to-kubernetes.fullname" . }}
  labels:
    app: {{ template "azure-key-vault-to-kubernetes.fullname" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    component: mutating
spec:
  type: {{ .Values.webhook.service.type }}
  ports:
    - port: {{ .Values.webhook.service.externalPort }}
      targetPort: {{ .Values.webhook.service.internalPort }}
      protocol: TCP
      name: {{ .Values.webhook.service.name }}
  selector:
    app: {{ template "azure-key-vault-to-kubernetes.name" . }}
    release: {{ .Release.Name }}
